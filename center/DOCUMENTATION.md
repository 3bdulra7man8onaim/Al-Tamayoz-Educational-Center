# 📚 التوثيق الشامل - مركز التميز التعليمي

---

## 📖 جدول المحتويات

1. [نظرة عامة](#نظرة-عامة)
2. [البدء السريع](#البدء-السريع)
3. [المميزات](#المميزات)
4. [إعداد Firebase](#إعداد-firebase)
5. [النشر](#النشر)
6. [التصميم والثيمات](#التصميم-والثيمات)
7. [حل المشاكل](#حل-المشاكل)
8. [الأوامر السريعة](#الأوامر-السريعة)

---

## 🎯 نظرة عامة

نظام شامل لإدارة المراكز التعليمية مع ميزات متقدمة للبحث والتتبع.

### المميزات الرئيسية:
- 🔍 بحث ذكي شامل (طلاب، مدرسين، مواد)
- 📚 إدارة كاملة للمواد والصفوف
- 👨‍🏫 إدارة المدرسين والأسعار
- 🕒 إدارة المجموعات والمواعيد
- 👥 إدارة الطلاب والحضور
- 💰 إدارة المدفوعات
- 📊 إدارة الدرجات
- 📱 إرسال رسائل واتساب
- 🌓 وضعين (فاتح/داكن)
- 🎨 تصميم عصري وجذاب

---

## 🚀 البدء السريع

### المتطلبات:
- حساب Firebase
- Firebase CLI

### خطوات الإعداد:

1. **تثبيت Firebase CLI**
```bash
npm install -g firebase-tools
```

2. **تسجيل الدخول**
```bash
firebase login
```

3. **تحديث بيانات Firebase**
افتح `app.js` وحدّث:
```javascript
const firebaseConfig = {
    apiKey: "YOUR_API_KEY",
    authDomain: "altamayoz-d343a.firebaseapp.com",
    projectId: "altamayoz-d343a",
    storageBucket: "altamayoz-d343a.firebasestorage.app",
    messagingSenderId: "YOUR_SENDER_ID",
    appId: "YOUR_APP_ID"
};
```

4. **تفعيل Firestore**
- افتح [Firebase Console](https://console.firebase.google.com/project/altamayoz-d343a)
- Build > Firestore Database
- Create Database
- اختر المنطقة: **europe-west**
- Start in production mode

5. **ضبط قواعد Firestore**
```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
```

6. **النشر**
```bash
firebase deploy
```

---

## ✨ المميزات

### 🔍 البحث الذكي

#### 1. البحث الشامل (الصفحة الرئيسية)
- يبحث في جميع الطلاب والمدرسين والمواد
- عرض النتائج مع التفاصيل
- انتقال تلقائي للصفحة المناسبة

#### 2. البحث في المادة
- يبحث في طلاب المادة المحددة

#### 3. البحث في المجموعة
- يبحث في طلاب المجموعة المحددة فقط
- بحث فوري أثناء الكتابة

### 📚 إدارة المواد
- إضافة مواد مخصصة لكل صف
- حذف المواد مع جميع البيانات المرتبطة
- لا توجد مواد افتراضية - يجب إضافتها يدوياً

### 👨‍🏫 إدارة المدرسين
- إضافة وتعديل بيانات المدرسين
- تحديد سعر الاشتراك الشهري
- **رقم هاتف المدرس** (إجباري) - لإرسال الإحصائيات
- ربط المدرس بالمادة والصف

### 🕒 إدارة المجموعات
- إضافة مجموعة واحدة تحتوي على عدة أيام
- اختيار أيام ومواعيد متعددة
- نظام 12 ساعة مع صباحاً/مساءً
- عرض المواعيد الكاملة

**مثال:**
- اختر: السبت 4:00 م + الثلاثاء 6:00 م
- النتيجة: مجموعة واحدة "السبت: 4:00 م | الثلاثاء: 6:00 م"

### 👥 إدارة الطلاب
- إضافة بيانات كاملة للطالب
- تسجيل الحضور والغياب مع التاريخ
- تتبع عدد الغيابات
- حفظ تاريخ آخر حضور

### 💰 إدارة المدفوعات
- **تسجيل فلوس شهر [الشهر الحالي]**
- المبلغ يُؤخذ تلقائياً من سعر المدرس
- لا حاجة لإدخال بيانات - فقط اضغط "تسجيل"
- عرض حالة الدفع (مدفوع/لم يدفع)
- تتبع آخر شهر مدفوع

### 📊 إدارة الدرجات
- إضافة درجات الامتحانات
- عرض سجل الدرجات لكل طالب

### 📱 التواصل عبر واتساب

#### 1. رسائل لأولياء الأمور
- إرسال حالة الطالب
- تحتوي على: المادة، المدرس، المجموعة، الغيابات، الدفع

#### 2. إحصائيات للمدرس
- **زر جديد**: "📊 إرسال إحصائيات للمدرس عبر واتساب"
- يُرسل على رقم المدرس المسجل
- يحتوي على:
  - قائمة بجميع الطلاب
  - عدد غيابات كل طالب
  - حالة دفع كل طالب
  - إجمالي الطلاب
  - عدد من دفع ومن لم يدفع

**ملاحظة:** الأرقام تُحول تلقائياً للصيغة الدولية (يضاف 2 قبل الرقم)

---

## 🔥 إعداد Firebase

### الخطوة 1: إنشاء المشروع
1. افتح [Firebase Console](https://console.firebase.google.com)
2. أنشئ مشروع جديد أو استخدم `altamayoz-d343a`

### الخطوة 2: تفعيل Firestore
1. Build > Firestore Database
2. Create Database
3. اختر الموقع: **europe-west**
4. Start in production mode
5. Enable

### الخطوة 3: الحصول على بيانات التكوين
1. اضغط على ⚙️ (Project Settings)
2. انزل لقسم "Your apps"
3. إذا لم يكن هناك Web App:
   - اضغط "Add app"
   - اختر Web (</>)
   - سمّه "Al Tamayoz"
4. انسخ `firebaseConfig`

### الخطوة 4: تحديث app.js
استبدل البيانات في `app.js`:
```javascript
const firebaseConfig = {
    apiKey: "YOUR_REAL_API_KEY",
    authDomain: "altamayoz-d343a.firebaseapp.com",
    projectId: "altamayoz-d343a",
    storageBucket: "altamayoz-d343a.firebasestorage.app",
    messagingSenderId: "YOUR_REAL_ID",
    appId: "YOUR_REAL_APP_ID"
};
```

### الخطوة 5: ضبط قواعد الأمان
في Firestore > Rules:
```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
```
اضغط **Publish**

---

## 🚀 النشر

### نشر كامل
```bash
firebase deploy
```

### نشر Hosting فقط
```bash
firebase deploy --only hosting
```

### نشر Rules فقط
```bash
firebase deploy --only firestore:rules
```

### بعد النشر
سيظهر لك:
```
✔  Deploy complete!

Hosting URL: https://altamayoz-d343a.web.app
```

---

## 🎨 التصميم والثيمات

### التصميم العصري
- ✅ خلفيات متدرجة جميلة
- ✅ شفافية وتأثيرات blur (Glassmorphism)
- ✅ ظلال ملونة وعميقة
- ✅ حركة سلسة وتفاعلية
- ✅ رسوم متحركة ناعمة
- ✅ تأثيرات hover جذابة

### نظام الثيمات

#### الوضع الفاتح (Light Mode)
- خلفية: تدرج أزرق-بنفسجي
- بطاقات: بيضاء شفافة
- نصوص: داكنة واضحة
- مناسب للنهار

#### الوضع الداكن (Dark Mode)
- خلفية: تدرج أسود-أزرق داكن
- بطاقات: رمادية داكنة شفافة
- نصوص: فاتحة واضحة
- مناسب لليل

#### التبديل بين الأوضاع
1. اضغط على زر الثيم في أعلى يسار الشاشة
2. 🌙 = الوضع الفاتح (اضغط للتحويل للداكن)
3. ☀️ = الوضع الداكن (اضغط للتحويل للفاتح)
4. ✅ يُحفظ اختيارك تلقائياً

---

## 🔧 حل المشاكل

### "حدث خطأ في الإضافة"
**الأسباب:**
- Firestore غير مفعّل
- بيانات `firebaseConfig` خاطئة
- قواعد Firestore خاطئة

**الحل:**
1. تأكد من تفعيل Firestore
2. تحقق من `firebaseConfig` في app.js
3. تحقق من قواعد Firestore: `allow read, write: if true;`

### "Permission denied"
**الحل:**
```javascript
// في Firestore Rules:
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
```

### "Error: HTTP Error: 403"
**الحل:**
```bash
firebase logout
firebase login
firebase projects:list
```

### واتساب لا يفتح
**السبب:** رقم الهاتف يحتاج كود الدولة

**الحل:** النظام يضيف كود الدولة (2) تلقائياً
- `01012345678` → `201012345678` ✅

### البيانات لا تظهر
**الحل:**
1. افتح Console (F12)
2. تحقق من رسائل الخطأ
3. تأكد من وجود بيانات في Firestore Console

---

## ⚡ الأوامر السريعة

### إعداد
```bash
npm install -g firebase-tools
firebase login
firebase projects:list
```

### النشر
```bash
firebase deploy                    # نشر كامل
firebase deploy --only hosting     # Hosting فقط
firebase deploy --only firestore   # Rules فقط
```

### الاختبار المحلي
```bash
firebase serve                     # تشغيل محلي
# افتح: http://localhost:5000
```

### المراقبة
```bash
firebase projects:list             # عرض المشاريع
firebase use                       # المشروع الحالي
```

---

## 📋 قائمة التحقق قبل النشر

### الإعداد
- [ ] تثبيت Firebase CLI
- [ ] تسجيل الدخول
- [ ] تفعيل Firestore
- [ ] الحصول على `firebaseConfig`
- [ ] تحديث app.js
- [ ] ضبط قواعد Firestore

### الاختبار
- [ ] إضافة مادة
- [ ] إضافة مدرس (مع رقم هاتف)
- [ ] إضافة مجموعة
- [ ] إضافة طالب
- [ ] البحث الشامل
- [ ] تسجيل الحضور
- [ ] تسجيل الدفع
- [ ] إرسال إحصائيات للمدرس
- [ ] تبديل الثيم

### النشر
- [ ] `firebase deploy`
- [ ] فتح الرابط المنشور
- [ ] اختبار جميع المميزات

---

## 📱 تنسيق أرقام الهواتف

### الإدخال
يمكنك إدخال الرقم بأي صيغة:
- `01012345678` ✅
- `1012345678` ✅
- `+201012345678` ✅

### التحويل التلقائي
النظام يحول الرقم تلقائياً:
- `01012345678` → `201012345678`
- `1012345678` → `21012345678`

### النتيجة
- ✅ رابط واتساب يفتح بشكل صحيح
- ✅ الرسالة تُرسل بدون مشاكل

---

## 🎯 نصائح مهمة

### للحصول على أفضل تجربة:
1. ✅ استخدم Chrome أو Safari
2. ✅ تأكد من اتصال الإنترنت
3. ✅ احفظ نسخة احتياطية من البيانات
4. ✅ اختبر على الهاتف قبل الاستخدام الفعلي

### الأمان:
⚠️ القواعد الحالية مفتوحة للجميع (للتطوير فقط)

للإنتاج، فعّل Firebase Authentication وحدّث القواعد:
```javascript
allow read, write: if request.auth != null;
```

---

## 📊 هيكل المشروع

```
altamayoz/
├── index.html          # الواجهة (7 صفحات + 8 نوافذ)
├── app.js             # المنطق (1200+ سطر)
├── style.css          # التنسيقات
├── firebase.json      # إعدادات Hosting
├── firestore.rules    # قواعد الأمان
├── .firebaserc        # تكوين المشروع
└── DOCUMENTATION.md   # هذا الملف
```

---

## 🎉 الخلاصة

المشروع جاهز 100% مع:
- ✅ بحث ذكي شامل
- ✅ إدارة كاملة للبيانات
- ✅ تصميم عصري وجذاب
- ✅ وضعين (فاتح/داكن)
- ✅ إحصائيات للمدرس عبر واتساب
- ✅ نظام 12 ساعة للمواعيد
- ✅ تسجيل دفع محسّن
- ✅ متجاوب 100%

---

## 📞 روابط مهمة

- **Firebase Console**: https://console.firebase.google.com/project/altamayoz-d343a
- **Firestore**: https://console.firebase.google.com/project/altamayoz-d343a/firestore
- **Hosting**: https://console.firebase.google.com/project/altamayoz-d343a/hosting
- **الموقع المنشور**: https://altamayoz-d343a.web.app

---

**بالتوفيق! 🚀**
